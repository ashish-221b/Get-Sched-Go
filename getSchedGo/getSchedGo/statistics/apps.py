from django.apps import AppConfig

## This is auto-generate django-python file that currently stores the name of app for server to recognise during hosting
class StatisticsConfig(AppConfig):
    name = 'statistics'
